/** Insertion sort
 *
 * - Useful when the list is almost sorted.
 * - O(1), when it's almost sorted
 * - Good with small data sets
 *
 */

const array = [94, 2, 44, 24, 9, 1, -1];

const insertionSort = (arr) => {
  // loop through array, i
  // loop through array, j
  //check if array[i] <= array[j] && array[i] > array[j-1]

  return arr;
};

console.log(insertionSort(array));
